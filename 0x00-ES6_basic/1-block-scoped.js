export default function taskBlock(trueOrFalse) {
  task = false;
  task2 = true;

  if (trueOrFalse) {
	   // eslint-disable-next-line
    var task;
	   // eslint-disable-next-line
    var task2;
  }

  return [task, task2];
}
